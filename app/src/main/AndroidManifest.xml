<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.legalisir">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pengiriman_Pembayaran"></activity>
        <activity android:name=".pembayaran_Tujuan" />
        <activity android:name=".detail_Pengambilan" />
        <activity android:name=".detail_Pengiriman" />
        <activity
            android:name=".bottomNav"
            android:label="@string/title_activity_bottom_nav" />
        <activity android:name=".Register" />
        <activity
            android:name=".InputLegalisir"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".input_file" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Splash"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>